
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Task</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <h2 class="text-center mt-4">Edit Task</h2>
    <form action="/updateData" method="post" class="m-auto border mt-3 p-4 rounded shadow" style="width: 30%;">
        <input type="hidden" name="id" value="<%= singleData.id %>">
        
        <label class="form-label">Title</label>
        <input type="text" name="title" value="<%= singleData.title %>" class="form-control" placeholder="Enter task title"> <br>

        <label class="form-label">Description</label>
        <input type="text" name="description" value="<%= singleData.description %>" class="form-control" placeholder="Enter task description"> <br>

        <label class="form-label">Priority</label>
        <select name="priority" class="form-select">
            <option value="High" <%= singleData.priority === 'High' ? 'selected' : '' %>>High</option>
            <option value="Medium" <%= singleData.priority === 'Medium' ? 'selected' : '' %>>Medium</option>
            <option value="Low" <%= singleData.priority === 'Low' ? 'selected' : '' %>>Low</option>
        </select> <br>

        <div class="form-check">
            <input type="checkbox" name="completed" class="form-check-input" <%= singleData.completed ? 'checked' : '' %> >
            <label class="form-check-label">Mark as Completed</label>
        </div> <br>

        <button type="submit" class="btn btn-primary w-100">Update Task</button>
    </form>
</body>
</html>
